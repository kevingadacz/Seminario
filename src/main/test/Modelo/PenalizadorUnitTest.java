package Modelo;

public class PenalizadorUnitTest {
    //Testear que se crea un cliente sin penalizaciones
    //Testear que autorizarTurno permite sacar el turno al cliente si tiene menos de 3 penalizaciones
    //Testear que autorizarTurno no permite sacar el turno al cliente si tiene mas de 2 penalizaciones
    //Testear que penalizar le agrega una penalizacion al cliente (se sugiere agregar tantas penalizaciones como sea
    // necesarias para que el cliente ya no pueda solicitarTurno y esperar una Exception)
}
